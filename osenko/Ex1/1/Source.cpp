#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int main() {
	int a = 55, b = 28, c = 2002, k = 36211;
	cout << pow(round(sqrt(k)*1)/1 - 1, 2) << " <= " << k << " < " << pow(round(sqrt(k) * 1) / 1 + 1, 2);

return 0;
}